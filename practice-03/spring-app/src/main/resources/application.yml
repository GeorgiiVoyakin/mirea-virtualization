spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:${PORT}/postgres
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create
